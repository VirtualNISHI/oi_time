requests>=2.31.0
tweepy>=4.14.0
python-dotenv>=1.0.0
matplotlib>=3.8
numpy>=1.26
# 共有 jp_translator (Gemini → OpenAI → Grok → DeepL)
google-genai>=1.0
httpx>=0.27
pydantic>=2.0
# 旧 openai_commentary.py 用 (削除済) — 共有モジュールが httpx 直接呼び出しに
# 置き換えたので必須ではないが、将来の独立利用向けに残す。
openai>=1.40
